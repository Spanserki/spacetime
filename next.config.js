/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'avatars.githubusercontent.com',
            'img.freepik.com',
            'ichef.bbci.co.uk',
        ]
    }
}

module.exports = nextConfig
